/**
 * Body intro: pinned section scroll length as a fraction of viewport height (`end: +=` on the intro pin).
 */
export var EPIC_INTRO_PIN_SCROLL_VH = 0.56;

/**
 * #story: max scroll distance (px) to scrub the service card Flip burst. Pinned to buy scrub room (cap 1600).
 */
export var EPIC_STORY_CARDS_SCRUB_PX = 1600;
